# ТЗ: Динамика багов в бэклоге по спринтам

## Цель
Реализовать сбор и отображение ретроспективных данных о количестве багов в бэклоге на момент завершения каждого спринта.

## Описание задачи

### Текущая ситуация
- График "Динамика багов в бэклоге по спринтам" показывает сообщение "Нет данных"
- В `config.json` поле `sprints` пустое: `"sprints": []`
- Компонент `SprintBacklogChart` готов к отображению данных, но данные не собираются

### Требуемая функциональность
Создать механизм ретроспективного анализа, который на момент завершения каждого спринта определяет:
1. Сколько багов было в статусе "To Do" (в бэклоге)
2. Сколько багов было в статусе "In Progress"
3. Общее количество открытых багов на дату завершения спринта

## Техническое задание

### 1. Структура данных

#### 1.1. Интерфейс данных спринта
```typescript
interface SprintBacklogData {
  sprint: string;           // Название спринта, например "Sprint 46"
  sprintId: number;         // ID спринта в Jira
  startDate: string;        // Дата начала спринта (ISO 8601)
  endDate: string;          // Дата окончания спринта (ISO 8601)
  backlogBugs: number;      // Количество багов в бэклоге на момент завершения
  inProgressBugs?: number;  // Опционально: баги в работе
  totalOpenBugs?: number;   // Опционально: всего открытых багов
}
```

#### 1.2. Обновление config.json
```json
{
  "lastUpdated": "2025-12-26T10:00:00Z",
  "jiraHost": "https://concertwithme.atlassian.net",
  "projectKey": "DCLS",
  "periods": [...],
  "components": [...],
  "sprints": [
    {
      "sprint": "Sprint 46",
      "sprintId": 123,
      "startDate": "2025-11-25",
      "endDate": "2025-12-08",
      "backlogBugs": 74,
      "inProgressBugs": 5,
      "totalOpenBugs": 79
    },
    // ... остальные спринты
  ]
}
```

### 2. Сбор данных из Jira

#### 2.1. Получение списка спринтов
**API Endpoint:**
```
GET /rest/agile/1.0/board/{boardId}/sprint?state=closed&maxResults=50
```

**Параметры:**
- `boardId`: ID доски (из .env: `JIRA_BOARD_ID=20`)
- `state=closed`: Только завершенные спринты
- `maxResults`: Количество спринтов (рекомендуется 50-100)

**Ответ:**
```json
{
  "values": [
    {
      "id": 123,
      "name": "Sprint 46",
      "state": "closed",
      "startDate": "2025-11-25T09:00:00.000Z",
      "endDate": "2025-12-08T17:00:00.000Z",
      "completeDate": "2025-12-08T17:30:00.000Z"
    }
  ]
}
```

#### 2.2. Получение количества багов на дату завершения спринта

**Проблема:** Jira API не предоставляет прямого способа получить исторические данные.

**Решение:** Использовать JQL с фильтрацией по датам:

**JQL запрос для багов в бэклоге:**
```jql
project = DCLS
AND type = Bug
AND created <= "2025-12-08 17:00"
AND (resolved IS EMPTY OR resolved > "2025-12-08 17:00")
AND status IN ("To Do", "Backlog", "Open")
```

**Логика:**
1. `created <= endDate` - баг был создан до завершения спринта
2. `resolved IS EMPTY OR resolved > endDate` - баг не был решен к моменту завершения
3. `status IN (...)` - баг находился в бэклоге

**Примечание:** Это приблизительная оценка, так как Jira не хранит полную историю изменения статусов.

### 3. Реализация в коде

#### 3.1. Обновить `scripts/jira/queries.ts`

Добавить функцию для получения багов на конкретную дату:

```typescript
export function buildBacklogBugsQuery(
  projectKey: string,
  endDate: Date
): string {
  const dateStr = endDate.toISOString().split('T')[0];

  return `
    project = ${projectKey}
    AND type = Bug
    AND created <= "${dateStr}"
    AND (resolved IS EMPTY OR resolved > "${dateStr}")
    AND status IN ("To Do", "Backlog", "Open", "New")
    ORDER BY created DESC
  `.trim();
}
```

#### 3.2. Обновить `scripts/jira/client.ts`

Добавить метод для получения спринтов:

```typescript
export async function fetchSprints(
  boardId: number,
  maxResults: number = 50
): Promise<JiraSprint[]> {
  const endpoint = `/rest/agile/1.0/board/${boardId}/sprint`;
  const params = new URLSearchParams({
    state: 'closed',
    maxResults: maxResults.toString(),
    orderBy: '-id' // Сортировка от новых к старым
  });

  const response = await jiraRequest(`${endpoint}?${params}`);
  return response.values;
}
```

#### 3.3. Обновить `scripts/fetch-jira.ts`

Добавить функцию сбора данных спринтов:

```typescript
async function collectSprintData(
  boardId: number,
  projectKey: string,
  sprintCount: number = 50
): Promise<SprintBacklogData[]> {
  console.log(`\n📊 Сбор данных спринтов (последние ${sprintCount})...`);

  // 1. Получить список спринтов
  const sprints = await fetchSprints(boardId, sprintCount);
  console.log(`Найдено спринтов: ${sprints.length}`);

  const sprintData: SprintBacklogData[] = [];

  // 2. Для каждого спринта получить количество багов
  for (const sprint of sprints) {
    if (!sprint.completeDate) continue;

    const endDate = new Date(sprint.completeDate);
    const jql = buildBacklogBugsQuery(projectKey, endDate);

    try {
      const bugs = await searchJira(jql, 0, 1000); // Получаем все баги

      sprintData.push({
        sprint: sprint.name,
        sprintId: sprint.id,
        startDate: sprint.startDate.split('T')[0],
        endDate: sprint.endDate.split('T')[0],
        backlogBugs: bugs.total
      });

      console.log(`  ✓ ${sprint.name}: ${bugs.total} багов в бэклоге`);

      // Задержка для избежания rate limit
      await delay(1000);

    } catch (error) {
      console.error(`  ✗ Ошибка при обработке ${sprint.name}:`, error);
    }
  }

  return sprintData.reverse(); // От старых к новым
}
```

#### 3.4. Интеграция в основной скрипт

```typescript
async function main() {
  console.log('🚀 Начинаем сбор данных из Jira...\n');

  // ... существующий код для периодов ...

  // Сбор данных спринтов
  const sprintData = await collectSprintData(
    Number(process.env.JIRA_BOARD_ID),
    process.env.JIRA_PROJECT_KEY!,
    50 // Последние 50 спринтов
  );

  // Обновление config.json
  const config: DashboardConfig = {
    lastUpdated: new Date().toISOString(),
    jiraHost: process.env.JIRA_HOST!,
    projectKey: process.env.JIRA_PROJECT_KEY!,
    periods: periodsConfig,
    components: [...uniqueComponents],
    sprints: sprintData // Добавляем данные спринтов
  };

  await saveJSON('src/data/config.json', config);
}
```

### 4. Ограничения и особенности

#### 4.1. Точность данных
⚠️ **Важно:** Данные будут приблизительными, потому что:
- Jira не хранит полную историю изменения статусов багов
- Невозможно точно определить, был ли баг в статусе "To Do" на конкретную дату
- Баги могли менять статус несколько раз

**Используемый подход:**
- Считаем баги, которые были **созданы до** завершения спринта
- И **не были решены** к моменту завершения спринта
- Это даёт общее представление о динамике

#### 4.2. Альтернативный подход (более точный, но сложный)

Использовать Jira Issue History API для получения истории изменений:

```
GET /rest/api/3/issue/{issueKey}/changelog
```

**Плюсы:**
- Точные данные о статусах на конкретную дату
- Можно отследить движение багов между статусами

**Минусы:**
- Требует дополнительный запрос для **каждого** бага
- При 1000+ багах = 1000+ запросов
- Высокий риск rate limiting
- Долгое время выполнения (часы)

**Рекомендация:** Начать с простого подхода (JQL), если нужна точность - реализовать changelog подход позже.

#### 4.3. Rate Limiting

При сборе данных для 50 спринтов:
- 1 запрос на список спринтов
- 50 запросов JQL для подсчёта багов
- **Итого: ~51 запрос**

С задержкой 1 секунда между запросами: **~1 минута**

### 5. Переменные окружения

Добавить в `.env.local`:

```env
# Количество спринтов для анализа
SPRINT_ANALYSIS_COUNT=50

# Включить/выключить сбор данных спринтов
COLLECT_SPRINT_DATA=true
```

### 6. Тестирование

#### 6.1. Локальное тестирование

```bash
# Запустить сбор данных
npm run fetch-jira

# Проверить config.json
cat src/data/config.json | grep -A 5 "sprints"

# Запустить дашборд
npm run dev
```

#### 6.2. Проверка данных

1. Открыть `src/data/config.json`
2. Убедиться, что массив `sprints` заполнен
3. Проверить корректность дат и количества багов

#### 6.3. Проверка графика

1. Открыть http://localhost:3000
2. Перейти на вкладку "Обзор"
3. Проверить график "Динамика багов в бэклоге по спринтам"
4. Убедиться, что данные отображаются корректно

## Оценка трудозатрат

| Задача | Время |
|--------|-------|
| Обновление типов и интерфейсов | 30 мин |
| Реализация API методов | 1 час |
| Интеграция в fetch-jira.ts | 1 час |
| Тестирование и отладка | 1 час |
| Документация | 30 мин |
| **Итого** | **4 часа** |

## Риски

1. **Rate Limiting** - при большом количестве спринтов может сработать ограничение
   - *Митигация:* Задержки между запросами, retry logic

2. **Неточные данные** - JQL подход не даёт 100% точности
   - *Митигация:* Документировать ограничения, рассмотреть changelog подход

3. **Старые спринты** - для очень старых спринтов данные могут быть неполными
   - *Митигация:* Ограничить анализ последними 50-100 спринтами

## Следующие шаги

1. ✅ Создать ТЗ (этот документ)
2. ⏳ Реализовать API методы для получения спринтов
3. ⏳ Добавить JQL запрос для подсчёта багов на дату
4. ⏳ Интегрировать в основной скрипт fetch-jira
5. ⏳ Протестировать сбор данных
6. ⏳ Проверить отображение на дашборде
7. ⏳ Обновить документацию

## Альтернативные решения

### Вариант 1: Хранить снапшоты вручную
Запускать скрипт раз в неделю и сохранять текущее состояние бэклога.

**Плюсы:** Точные данные
**Минусы:** Нет исторических данных

### Вариант 2: Использовать Jira Insights/Reports API
Если у вас Jira Premium, можно использовать готовые отчёты.

**Плюсы:** Точные данные от Jira
**Минусы:** Требует Premium подписку

### Вариант 3: Парсить Velocity Chart
Использовать API для получения данных Velocity Chart.

**Плюсы:** Данные уже есть в Jira
**Минусы:** API может быть недоступен или ограничен

## Дополнительные материалы

- [Jira Agile REST API](https://docs.atlassian.com/jira-software/REST/latest/)
- [Jira JQL Reference](https://support.atlassian.com/jira-service-management-cloud/docs/use-advanced-search-with-jira-query-language-jql/)
- [Issue History API](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issues/#api-rest-api-3-issue-issueidorkey-changelog-get)
