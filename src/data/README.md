# Папка данных (src/data)

Эта папка используется для хранения данных в зависимости от выбранного режима работы.

## Режимы работы

### 🌐 Cloudflare режим (`DATA_SOURCE=cloudflare`)

В этом режиме данные загружаются из Cloudflare KV API через хуки.

**Папка data содержит только заглушки:**
- `config.json` - минимальная заглушка для TypeScript
- `periods/.gitkeep` - для сохранения структуры папок

**Настройка в `.env.local`:**
```env
NEXT_PUBLIC_DATA_SOURCE=cloudflare
NEXT_PUBLIC_API_URL=https://your-worker.workers.dev
```

### 💾 Local режим (`DATA_SOURCE=local`)

В этом режиме данные загружаются из локальных файлов.

**Настройка в `.env.local`:**
```env
NEXT_PUBLIC_DATA_SOURCE=local
```

**Папка data должна содержать:**
- `config.json` - конфигурация дашборда с периодами
- `periods/` - папка с файлами периодов (`period1.json`, `period2.json`, и т.д.)

**Генерация данных:**
```bash
npm run fetch-jira
```
